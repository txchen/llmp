services:
  llm-proxy:
    image: ghcr.io/txchen/llmp:latest
    ports:
      - "33000:33000"
    environment:
      ADMIN_PASSWORD: "${ADMIN_PASSWORD:?Set ADMIN_PASSWORD}"
    volumes:
      - llmp-data:/app/data
volumes:
  llmp-data:
