# The only required setting. Configure upstream services and member keys in the UI.
ADMIN_PASSWORD=

# Optional deployment overrides:
# PORT=33000
# DATABASE_PATH=./data/llmp.sqlite
# ADMIN_ORIGIN=https://ai.example.com
